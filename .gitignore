__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
dist/
build/
*.spec
.idea/
.vscode/
# logs and bug dumps contain captured user text — never publish
*.log
*.log.*
bugs/
bonjur-autostart.vbs

# vendored MCP tool schemas — not part of this product
mcps/

# agent scratch dirs — transcripts quote source, paths and sometimes tokens
.claude/
.grok/
.opencode/
.cursor/

# Windows shortcuts embed the machine name, volume label and absolute paths
*.lnk

# scratch notes / raw dumps
notes/public-apis-catalog/

# real AI endpoint config — corporate hosts/GUIDs, lives in ~/.bonjur-epta/
ai.json
# employer-specific acronym markers, same deal
internal_markers.json
.pytest_cache/

# launcher build artifacts (regenerated by scripts/release.ps1)
launcher/bin/
launcher/obj/
launcher/app.zip
release/
